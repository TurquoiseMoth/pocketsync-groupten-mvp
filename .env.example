# Local development (default) — requests go through the Vite proxy to localhost:5000
# VITE_API_BASE_URL=/api/v1

# Production / Render (set when deploying the frontend)
VITE_API_BASE_URL=https://pocketsync-backend-staging.onrender.com/api/v1

#to deploy the frontend, simply change VITE_API_BASE_URL to the production URL.this can be done, afterwards run the command

#yarn render deploy

