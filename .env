# Local dev: Vite proxies /api → http://localhost:5000
# VITE_API_BASE_URL=/api/v1

# Production / Render (set when deploying the frontend)
# Leave unset so the frontend calls /api/v1 and Vercel proxies API requests to Render.
VITE_API_BASE_URL=https://pocketsync.onrender.com/api/v1
